<%= render 'slides' %>

<%= render 'countdown' if @encuentro.show_countdown? %>

<%= render 'participantes' %>

<%= render 'instagram' if @encuentro.show_instagram_feed? %>

<%= render 'calendario' %>

<%= render 'past_events' if @encuentro.show_past_events? %>

<%= render 'footer' %>

<%= render 'copyright' %>
